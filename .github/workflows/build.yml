name: Build reVC macOS Native

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install dependencies
        run: brew install cmake glfw mpg123 pkg-config

      - name: Configure CMake
        run: |
          mkdir build
          cd build
          cmake \
            -D LIBRW_PLATFORM=GL3 \
            -D RE3_AUDIO=OPENAL \
            -D RE3_VENOM=ON \
            -D OPENAL_INCLUDE_DIR=/System/Library/Frameworks/OpenAL.framework/Headers \
            -D OPENAL_LIBRARY=/System/Library/Frameworks/OpenAL.framework \
            -D CMAKE_CXX_FLAGS="-I/System/Library/Frameworks/OpenAL.framework/Headers -std=c++11 -Wno-error" \
            ..

      - name: Build reVC
        run: |
          cd build
          make -j$(sysctl -n hw.ncpu)

      - name: Upload Executable Artifact
        uses: actions/upload-artifact@v4
        with:
          name: reVC-macOS-executable
          path: build/bin/reVC
